Package: wyntonquery
====================

Version: 0.0.0-9001 [2019-06-06]

SIGNIFICIANT CHANGES:

 * host_info() now reports on 'cpu_model', which is the corresponds to
   the cpu_info() field 'model_name', e.g.
   "Intel(R) Core(TM) i7-8650U CPU @ 1.90GHz".  Previously, it only reported
   on the CPU speed which was incorrectly inferred from the cpu_info() field
   'cpu_mhz', which fluctuates with the CPU load etc.

NEW FEATURES:

 * on_hostname() gained argument 'on_error' for controlling how errors should
   be handled.
 
BUG FIXES:

 * cpu_info() did not handle upper-case letters in field names.
 

Version: 0.0.0-9000 [2018-06-10]

NEW FEATURES:

 * Add host_table().
 
 * Add round_ram().
 
 * Add lscpu().
 
 * Add system_info().
 
 * Add disk_free().
 
 * Add queues().

 * Add on_hostname().
 
 * Add qhost() and cpu_info().
